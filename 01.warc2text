#!/bin/bash
set -euo pipefail

batch="$2"
output="$1/$(basename $batch)"

mkdir -p $output.$$

warc2text \
	--verbose \
	--tag-filters $SCRIPTS/mt-filter-list.annotated \
	--output $output.$$ \
	--pdfpass $output.$$/pdf.warc.gz \
	$batch/*.warc.gz

mv $output.$$ $output
